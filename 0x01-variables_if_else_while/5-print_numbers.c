#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints out decimal numbers form 0-9
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num = 0;

	while (num <10)
	{
		printf(num);
	}

	putchar('\n');

	return (0);
}
